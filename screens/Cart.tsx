import React from "react";
import {
  StyledText,
  StyledView,
} from "../components/styledComponents/StyledComponents";

const Cart = () => {
  return (
    <StyledView>
      <StyledText>Cart</StyledText>
    </StyledView>
  );
};

export default Cart;
